# Runtime dependencies (Lambda package - keep minimal)
# NOTE: comments kept ASCII so `sam build` (pip) does not fail under cp949 locale.
boto3>=1.34.0           # AWS Secrets Manager / DynamoDB / Lambda invoke
httpx>=0.27.0           # Naver / Telegram HTTP client (do not add other HTTP clients)
anthropic>=0.49.0       # Claude API - review analysis
python-dotenv>=1.0.0    # local .env loading (config dual-load path)
